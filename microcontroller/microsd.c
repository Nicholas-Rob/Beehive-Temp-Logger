#include "microsd.h"

void AppendMicroSD(char* time, float temperature){

    /*
    Write(time[0]); // Minute
    Write(time[1]); // Hour
    Write(time[2]); // Day
    Write(time[3]); // Month
    Write(time[4]); // Year

    Write(temperature);
    */

}

